<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    >

    <!-- Hình ảnh nhỏ ở giữa phía trên cùng -->

    <!-- Container chứa 3 button dọc ở giữa màn hình -->

    <ImageView
        android:id="@+id/smallImageView"
        android:layout_width="184dp"
        android:layout_height="159dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"

        android:layout_alignParentEnd="true"
        android:layout_marginStart="106dp"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="121dp"
        android:background="@drawable/snapedit_1699811681012" />

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="203dp"
        android:layout_height="279dp"
        android:layout_below="@+id/smallImageView"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="100dp"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="108dp"
        android:layout_marginBottom="194dp"
        android:orientation="vertical">

        <!-- Button 1 -->
        <Button
            android:id="@+id/buttonThemNV"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:background="@drawable/button_gradient"
            android:text="Đăng ký Nhân viên" />

        <!-- Button 2 -->
        <Button
            android:id="@+id/buttonQuetMa"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:background="@drawable/button_gradient"
            android:text="Quét mã QR" />

        <Button
            android:id="@+id/buttonXem"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:background="@drawable/button_gradient"
            android:text="Chọn ngày xem danh sách điểm danh" />

        <!-- Button 3 -->
    </LinearLayout>

</RelativeLayout>
